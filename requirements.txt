# =========================================================
# Pipeline SECOP I — Dependencias
# =========================================================
# Instalar con:  pip install -r requirements.txt
# =========================================================

# --- Web Scraping ---
selenium>=4.15.0,<5.0.0
webdriver-manager>=4.0.0,<5.0.0

# --- Parsing HTML ---
beautifulsoup4>=4.12.0,<5.0.0

# --- Datos ---
pandas>=2.1.0,<3.0.0
pyarrow>=14.0.0,<18.0.0       # Motor para Parquet

# --- Utilidades (opcionales pero recomendadas) ---
# lxml>=5.0.0                  # Parser alternativo para BeautifulSoup (más rápido)
# openpyxl>=3.1.0              # Exportación a Excel .xlsx
